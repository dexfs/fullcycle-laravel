steps:

- id: "Rodando docker-compose"
  name: 'gcr.io/$PROJECT_ID/docker-compose'
  args: ['-f', 'docker-compose.cloudbuild.yaml', 'up', '-d']

- id: "Rodando docker ps"
  name: 'docker'
  args: ['ps']

- id: "Rodando composer"
  name: 'docker'
  args: ['exec', '-t', 'app', 'composer', 'install']

- id: "Copiando .env"
  name: 'docker'
  args: ['exec', '-t', 'app', 'cp', '.env.example', '.env']

- id: "Rodando key:generate"
  name: 'docker'
  args: ['exec', '-t', 'app', 'php', '/var/www/artisan', 'key:generate']

- id: "Rodando migrations"
  name: 'docker'
  args: ['exec', '-t', 'app', 'php', '/var/www/artisan', 'migrate']

- id: "Rodando PHPUNIT"
  name: 'docker'
  args: ['exec', '-t', 'app', 'php', '/var/www/vendor/bin/phpunit', '-c', '/var/www/phpunit.xml']

- id: "Building image from Dockerfile.prod"
  name: 'docker'
  args: ['build', '-t', 'grc.io/$PROJECT_ID/app-laravel:$SHORT_SHA', '-f', 'Dockerfile.prod', '.']
# images: ['grc.io/$PROJECT_ID/app-laravel:$SHORT_SHA']

- id: "Pushing image"
  name: 'gcr.io/cloud-builders/docker'
  args: ['push', "grc.io/$PROJECT_ID/app-laravel:$SHORT_SHA"]

# - id: "Adjusting deployment file - k8s"
#   name: 'gcr.io/cloud-builders/gcloud'
#   entrypoing: '/bin/sh'
#   args:
#     - '-c'
#     - |
#       sed 's#img-app-deployment#grc.io/$PROJECT_ID/app-laravel:$SHORT_SHA#g' .k8s/app/deployment.yaml > k8s/app/deployment-new.yaml;
#       cat k8s/app/deployment-new.yaml;

